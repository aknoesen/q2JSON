cmd: 

Navigate to your project directory
cd path\to\your\q2json-streamlit

# Activate virtual environment
venv\Scripts\activate.bat

# Install requirements (if not already done)
pip install -r requirements.txt

# Run the app
streamlit run app.py





and then 

streamlit run app.py


Option 1: Open a NEW cmd window (Recommended)

Keep the stuck cmd window open (don't close it)
Open a NEW cmd window (Windows key + R → type cmd → Enter)
Navigate to your project folder:
cmd  cd path\to\your\q2json-stream

kill-streamlit.bat